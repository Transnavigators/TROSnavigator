map_type: costmap
obstacle_range: 6.0
raytrace_range: 3.0
transform_tolerance: 0.2
footprint: [[0.4, 0.42], [0.4, -0.4], [-0.4, 0.42], [-0.4, -0.4]
inflation_radius: 10.0

observation_sources: laser_scan_sensor

laser_scan_sensor: {sensor_frame: neato_laser,
                    data_type: LaserScan,
                    expected_update_rate: 0.3,
                    topic: /scan,
                    marking: true,
                    clearing: true,
                    min_obstacle_height: -0.10,
                    max_obstacle_height: 2.0}